# Copier ce fichier en .env et remplir les valeurs

# Générer avec : python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=remplacer-par-une-vraie-cle-secrete

# True en dev, False en production
DEBUG=True

# En prod : sengadget.sn,www.sengadget.sn
ALLOWED_HOSTS=localhost,127.0.0.1

# Laisser vide pour utiliser le backend console en dev
EMAIL_HOST_USER=sengadget.sn@gmail.com
EMAIL_HOST_PASSWORD=
